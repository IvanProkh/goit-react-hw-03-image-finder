import React from 'react';

const ImageGallery = () => {
  return (
    <ul className="Gallery">
      {/* {items.map(({ }) => (
        <li key={id}>
          <span>{name} :</span>
          <span>{number}</span>
        </li>
      ))} */}
    </ul>
  );
};

export default ImageGallery;
