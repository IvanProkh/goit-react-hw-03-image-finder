import React from 'react';
import { Gallery } from './ImageGallery.styled';

export const ImageGallery = () => {
  return (
    <Gallery>
      {/* {items.map(({ }) => (
        <li key={id}>
          <span>{name} :</span>
          <span>{number}</span>
        </li>
      ))} */}
    </Gallery>
  );
};
